import 'package:flutter/material.dart';

class ServiceUrl extends StatelessWidget {
  const ServiceUrl({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
